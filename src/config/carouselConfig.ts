// Configuración del carrusel (duraciones en ms)
const CAROUSEL_CONFIG = {
  // tiempo entre cambios (ms)
  IMAGE_CHANGE_INTERVAL: 5000,
  // duración del fade-to-black y fade-from-black (ms)
  OVERLAY_DURATION: 1000,
};

export default CAROUSEL_CONFIG;
